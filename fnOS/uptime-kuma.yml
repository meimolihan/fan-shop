services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    container_name: uptime-kuma
    ports:
      - 3001:3001
    volumes:
      - /vol1/1000/Docker/uptime-kuma/data:/app/data
    restart: always
    network_mode: bridge
